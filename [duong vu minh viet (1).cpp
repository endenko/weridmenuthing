#include <iostream>
#include <iomanip>
#include <math.h>
#include <stdio.h>
using namespace std;
int main ()
{
	int a,b,c; 
	cin>>a>>b>>c;
	int max = 0;
	if(max < a) max = a;
	if(max < b) max = b;
	if(max < c) max = c;
	cout<<"so lon nhat : "<<max; 
} 

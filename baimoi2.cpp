#include <iostream>
#include <iomanip>
#include <math.h>
#include <stdio.h>
using namespace std;
int main ()
{
	double a1,a2,S;
 	cin>>a1>>a2;
	S = sqrt(a1 *a2 + a2 *a2);
	cout<<fixed<<setprecision(2); 
	cout<<S; 
}

